<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 存储权限：用于备份和恢复数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".MoneyTrackerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneyTracker"
        tools:targetApi="31">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MoneyTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.transactions.AddTransactionActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.MoneyTracker" />
        <activity
            android:name=".ui.categories.CategoryManagerActivity"
            android:exported="false"
            android:theme="@style/Theme.MoneyTracker" />
        <activity
            android:name=".ui.transactions.AllTransactionsActivity"
            android:exported="false"
            android:theme="@style/Theme.MoneyTracker" />
        <activity
            android:name=".ui.settings.ThemeSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.MoneyTracker" />
        <activity
            android:name=".ui.settings.AccentColorSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.MoneyTracker" />
        <activity
            android:name=".ui.statistics.CategoryTransactionsActivity"
            android:exported="false"
            android:theme="@style/Theme.MoneyTracker" />
    </application>

</manifest>
